findByTeamId:
SELECT *
FROM ChannelEntity
WHERE teamId = :teamId
ORDER BY name ASC;

findByTeamAndSlug:
SELECT *
FROM ChannelEntity
WHERE teamId = :teamId
AND slug = :slug;

create:
INSERT INTO ChannelEntity(id, teamId, name, slug) VALUES ?;

deleteById:
DELETE FROM ChannelEntity WHERE id = :id;
