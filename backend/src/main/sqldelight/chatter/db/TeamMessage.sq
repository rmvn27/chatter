findByTimestamp:
SELECT *
FROM MessageEntity
WHERE channelId = :channelId
AND timestamp < :timestamp
ORDER BY timestamp DESC
LIMIT :limit;

create:
INSERT INTO MessageEntity(id, userId, channelId, type, content, timestamp) VALUES ?;
